services:
  db:
    image: mongo:7
    container_name: cashflow_db
    command: ["--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - db_data:/data/db
volumes:
  db_data:

